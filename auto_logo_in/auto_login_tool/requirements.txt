# 校园网自动登录工具依赖
# 核心依赖（必需）
pyautogui>=0.9.54
pywinauto>=0.6.8
psutil>=5.9.6
requests>=2.31.0
ping3>=4.0.5
win10toast>=0.9

# 可选依赖（功能已简化，不再必需）
# cryptography>=42.0.5  # 密码加密功能已移除

# 标准库依赖（Python内置，列出以供参考）
# configparser>=5.3.0  # Python 3.2+ 内置

# 开发依赖
pytest>=7.4.4
pytest-mock>=3.12.0

# 安装命令：
# pip install -r requirements.txt
# 或仅安装核心依赖：
# pip install pyautogui pywinauto psutil requests ping3 win10toast